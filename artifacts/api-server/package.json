{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ./dev.mjs",
    "dev:pg": "cross-env NODE_ENV=development PORT=8080 pnpm run build && cross-env NODE_ENV=development PORT=8080 pnpm run start",
    "dev:sqlite": "cross-env SQLITE_PATH=../../.local/farmacy-flow.sqlite pnpm --filter @workspace/db run push:sqlite && cross-env NODE_ENV=development PORT=8080 SQLITE_PATH=../../.local/farmacy-flow.sqlite pnpm run build:sqlite && cross-env NODE_ENV=development PORT=8080 SQLITE_PATH=../../.local/farmacy-flow.sqlite pnpm run start:sqlite",
    "build": "node ./build.mjs",
    "build:sqlite": "node ../desktop/build-api-desktop.mjs",
    "start": "node --enable-source-maps ./dist/index.mjs",
    "start:sqlite": "node --enable-source-maps ./dist-desktop/index.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@workspace/api-zod": "workspace:*",
    "@workspace/db": "workspace:*",
    "cookie-parser": "^1.4.7",
    "cors": "^2",
    "drizzle-orm": "catalog:",
    "express": "^5",
    "pino": "^9",
    "pino-http": "^10"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "cross-env": "^10.1.0",
    "esbuild": "^0.27.3",
    "esbuild-plugin-pino": "^2.3.3",
    "pino-pretty": "^13",
    "thread-stream": "3.1.0"
  }
}
